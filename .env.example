# Database Configuration
# Copy this file to .env and fill in your actual values
# NEVER commit .env to version control

# PostgreSQL Connection
DB_HOST=localhost
DB_PORT=5432
DB_NAME=photo_library
DB_USER=postgres
DB_PASSWORD=your_password_here

# Optional: Connection pool settings
DB_POOL_SIZE=5
DB_MAX_OVERFLOW=10

# OpenAI API (for AI tagging - may already be set)
# OPENAI_API_KEY=your_openai_key_here

# Shutterstock Integration (session cookie from browser)
# To get this cookie:
# 1. Log into https://submit.shutterstock.com in Chrome
# 2. Open DevTools (F12) -> Application -> Cookies
# 3. Find the 'session' cookie and copy its value
# SHUTTERSTOCK_SESSION=your_session_cookie_here
